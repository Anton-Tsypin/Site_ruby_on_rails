<body>
    
    <div class="base_form">
        <a style="font-size:28px">Пользователи: <%= Post.count %></a>

        <ul>
        <% @posts.each do |post| %>
            <li>
                <a class="large_text" style="color: rgb(0, 0, 50);" href="/users/<%= post.user_id %>"><%= post.title %></a>
            </li>
        <% end %>
        </ul>

    </div>
</body>